import { Plugin } from "@opencode-ai/plugin"

/** Declares the trajectory package; all current behavior is provided by its TUI entrypoint. */
export default Plugin.define({
  id: "opencode-trajectory",
  setup() {},
})
