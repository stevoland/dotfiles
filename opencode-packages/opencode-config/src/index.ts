import { Plugin } from "@opencode-ai/plugin"

/** Enables the `opencode-config` TUI plugin, including its skills sidebar control. */
export default Plugin.define({
  id: "opencode-config",
  tui: true,
  setup() {},
})
