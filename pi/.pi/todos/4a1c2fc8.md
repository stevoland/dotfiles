{
  "id": "4a1c2fc8",
  "title": "Add github-copilot/gpt-5.5 custom model definition to pi",
  "tags": [
    "pi",
    "models",
    "github-copilot"
  ],
  "status": "done",
  "created_at": "2026-04-28T13:35:33.982Z"
}

Added `agent/models.json` with a custom `github-copilot/gpt-5.5` model definition, copied the required GitHub Copilot base URL and headers from the built-in `gpt-5.4` entry, and added `github-copilot/gpt-5.5` to `agent/settings.json` `enabledModels`.

Validation performed:
- `ModelRegistry.find('github-copilot', 'gpt-5.5')` returns the custom model with no models.json errors.
- `ModelRegistry.getAvailable()` includes `github-copilot/gpt-5.5`.
- `pi --list-models gpt-5.5` shows the model.
- `pi --model github-copilot/gpt-5.5 --thinking low "Reply with exactly OK"` returned `OK`.
